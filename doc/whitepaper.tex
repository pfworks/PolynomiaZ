\documentclass[11pt,a4paper]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{float}

\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}

\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  backgroundcolor=\color{gray!10}
}

\title{\textbf{PolynomiaZ: Lossless Compression via\\Mathematical Function Fitting on Virtual Disk Platters}}
\author{Rory Toma}
\date{May 2026}

\begin{document}
\maketitle

\begin{abstract}
We present PolynomiaZ (PLTZ), a lossless compression codec that maps input data onto a virtual disk platter geometry and detects mathematical patterns within each track. Tracks matching a known pattern type are stored as compact function descriptors rather than raw bytes. A cross-track optimization pass further compresses groups of tracks with identical or linearly-varying parameters into single meta-descriptors. PLTZ achieves 5--12$\times$ better compression than zlib on linearly structured numerical data (firmware images, satellite telemetry, SPICE ephemeris kernels) while maintaining less than 1\% overhead on incompressible data. The codec supports adaptive sector sizing, multi-platter splitting, configurable raw fallback compression, and word-width-aware analysis for 16/32/64-bit numerical arrays.
\end{abstract}

\section{Introduction}

Standard compression algorithms (zlib, bzip2, LZMA) operate by identifying byte-level repetition through sliding windows, Burrows-Wheeler transforms, or dictionary coding. These approaches excel at data with repeated substrings---natural language text, markup, log files---but fundamentally cannot recognize that a byte sequence like $[0, 1, 2, 3, \ldots, 255]$ represents a linear function. They see ``no repeated substrings'' and store it nearly verbatim.

PolynomiaZ takes a different approach: it treats the input as data written to a virtual disk platter and asks, for each track, ``what mathematical function produced these bytes?'' When a function is found, the track is stored as a compact descriptor (e.g., \texttt{LINEAR(start=0, step=1)} in 4 bytes instead of 256 raw bytes).

This approach is not general-purpose. It targets a specific domain: data with known mathematical structure arising from physical processes, calibration sequences, address tables, and numerical computation.

\section{Architecture}

\subsection{Disk Platter Model}

Input data of length $N$ bytes is mapped onto a virtual disk with geometry $(T, S)$ where $T$ is the number of concentric tracks and $S$ is the number of sectors per track, such that $T \times S \geq N$. Each byte at sequential position $i$ is assigned to track $\lfloor i / S \rfloor$ at sector position $(i \bmod S)$. This partitioning determines how the data is divided into chunks for pattern analysis.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=1.0]
  % Draw concentric tracks
  \foreach \r in {1.2, 1.8, 2.4, 3.0} {
    \draw[gray!60] (0,0) circle (\r);
  }
  % Track labels with arrows pointing to their ring (bottom right)
  \draw[<-, gray!80] (3.0, -0.6) -- (3.8, -0.6) node[right, font=\footnotesize, black] {Track 0};
  \draw[<-, gray!80] (2.4, -0.6) -- (3.8, -1.2) node[right, font=\footnotesize, black] {Track 1};
  \draw[<-, gray!80] (1.8, -0.6) -- (3.8, -1.8) node[right, font=\footnotesize, black] {Track 2};
  \draw[<-, gray!80] (1.2, -0.6) -- (3.8, -2.4) node[right, font=\footnotesize, black] {Track 3};
  % Draw sector lines
  \foreach \angle in {0,45,...,315} {
    \draw[gray!30] (0,0) -- (\angle:3.2);
  }
  % Label sectors outside outer ring
  \foreach \s/\angle in {0/67.5, 1/22.5, 2/337.5, 3/292.5, 4/247.5, 5/202.5, 6/157.5, 7/112.5} {
    \node[font=\footnotesize] at (\angle:3.6) {S\s};
  }
  % Highlight one sector
  \fill[blue!20] (0:2.7) arc (0:45:2.7) -- (45:3.3) arc (45:0:3.3) -- cycle;
  \draw[blue!60, thick] (0:2.7) arc (0:45:2.7) -- (45:3.3) arc (45:0:3.3) -- cycle;
  \node[font=\footnotesize, blue] at (22.5:4.0) {byte $i$};
  % Center
  \fill (0,0) circle (0.05);
\end{tikzpicture}
\caption{Disk platter model: input bytes are arranged in concentric tracks divided into sectors. The geometry determines how data is partitioned for pattern analysis.}
\end{figure}

\subsection{Adaptive Geometry Selection}

The codec evaluates multiple sector sizes $S \in \{8, 16, 32, 64, 128, 256, 512\}$ and selects the geometry that produces the smallest compressed output. Different data benefits from different geometries: small sectors expose patterns in structured headers, while large sectors capture long arithmetic sequences.

\subsection{Multi-Platter Splitting}

After analysis, tracks are separated into two platters:
\begin{itemize}
\item \textbf{Platter 0}: Tracks with detected patterns (structured data) and meta-groups
\item \textbf{Platter 1}: Tracks with no pattern (raw data, optionally compressed with deflate)
\end{itemize}

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=0.8, every node/.style={font=\small}]
  % Input
  \node[draw, minimum width=6cm, minimum height=1cm] (input) at (0,6) {Input Data};

  % Analyzer
  \node[draw, rounded corners, fill=gray!10, minimum width=4cm, minimum height=0.8cm] (analyze) at (0,4.5) {Pattern Analyzer};

  % Platter 0
  \node[draw, rounded corners, fill=blue!10, minimum width=5cm, minimum height=3.2cm] (p0) at (-3.5,0.5) {};
  \node[font=\small\bfseries, anchor=north] at (-3.5, 1.9) {Platter 0 (Structured)};
  \node[font=\scriptsize, align=left, anchor=north] at (-3.5, 1.3) {Track 0: CONST(0xFF)\\Track 1: LINEAR(0, 1)\\Track 3: LINEAR\_WIDE(...)\\Meta: tracks 4--19 AllSame};

  % Platter 1
  \node[draw, rounded corners, fill=red!10, minimum width=5cm, minimum height=3.2cm] (p1) at (3.5,0.5) {};
  \node[font=\small\bfseries, anchor=north] at (3.5, 1.9) {Platter 1 (Raw)};
  \node[font=\scriptsize, align=left, anchor=north] at (3.5, 1.3) {Track 2: RAW (deflated)\\Track 20: RAW (deflated)\\Track 21: RAW (deflated)};

  % Arrows
  \draw[->, thick] (input) -- (analyze);
  \draw[->, thick] (-1,4.1) -- (-1,3.2) -- (-3.5,3.2) -- (-3.5,2.1);
  \draw[->, thick] (1,4.1) -- (1,3.2) -- (3.5,3.2) -- (3.5,2.1);
  % Labels above the horizontal lines
  \node[font=\scriptsize, above] at (-2.25,3.2) {patterns found};
  \node[font=\scriptsize, above] at (2.25,3.2) {no pattern};
\end{tikzpicture}
\caption{Multi-platter splitting: tracks with detected mathematical patterns are stored on Platter 0 with compact function descriptors; unmatched tracks go to Platter 1 with optional deflate compression.}
\end{figure}

\subsection{Cross-Track Meta-Descriptors}

After per-track analysis, groups of four or more tracks sharing the same pattern type are examined for parameter-level patterns. When found, the entire group is encoded as a single meta-descriptor:

\begin{itemize}
\item \textbf{AllSame} --- $N$ tracks with identical parameters stored once. Cost: $O(N)$ for indices + $O(1)$ for parameters, vs.\ $O(N \times p)$ individually.
\item \textbf{ConstLinear} --- $N$ CONST tracks whose values form an arithmetic sequence. Stored as start + step (2 bytes) instead of $N$ individual values.
\item \textbf{LinearStartsLinear} --- $N$ LINEAR tracks with the same step but linearly-incrementing start values. Stored as base\_start + start\_step + step (6 bytes).
\end{itemize}

This optimization is particularly effective for firmware images with large padding regions (many identical CONST tracks) and data with systematic ramps across tracks.

\section{Pattern Detection}

Each track is tested against pattern detectors in priority order. When both RLE and REPEAT match, the smaller encoding is chosen.

\subsection{Pattern Types}

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Tag} & \textbf{Pattern} & \textbf{Detection Criterion} & \textbf{Storage Cost} \\
\midrule
0 & CONST & All bytes identical & 1 byte \\
1 & LINEAR & $x_i = a + bi$ for all $i$ & 4 bytes \\
2 & RLE & Run-length encoding saves space & $2n_{\text{runs}}$ bytes \\
3 & REPEAT & Repeating sub-pattern of period $p < S/3$ & $p$ bytes \\
4 & DELTA & $\leq 8$ unique deltas, all in $[-128, 127]$ & $S+1$ bytes \\
5 & PERIODIC & FFT with $k$ significant components & $4 + 18k$ bytes \\
6 & POLY & Polynomial degree $\leq 8$ exact fit & $1 + 8(d+1)$ bytes \\
9 & LINEAR\_WIDE & Linear sequence in 16/32-bit words & 19 bytes \\
10 & LINEAR\_F64 & Linear sequence in IEEE 754 doubles & 18 bytes \\
11 & DELTA\_F64 & f64 values with f32-representable deltas & $10 + 4(n-1)$ bytes \\
13 & XOR\_MASK & Data XORed with constant reveals simpler pattern & $2 +$ inner bytes \\
14 & SPARSE & Mostly one value with few exceptions & $3 + 3e$ bytes \\
15 & PIECEWISE\_LINEAR & 2--4 linear segments & $1 + 6s$ bytes \\
16 & MIRROR & Second half is reverse of first & $S/2$ bytes \\
17 & INTERLEAVED\_LINEAR & Two interleaved arithmetic sequences & 10 bytes \\
18 & EXPONENTIAL & Geometric sequence $a \cdot r^i$ & 10 bytes \\
19 & DELTA\_RLE & Run-length encoded deltas (staircases) & $3 + 2r$ bytes \\
20 & BIT\_PACKED & Values fit in $<$7 bits, stored packed & $1 + \lceil nB/8 \rceil$ bytes \\
21 & DIFF\_TABLE & Nth-order constant differences & $5$--$9$ bytes \\
22 & MOD\_ARITH & Values follow $(a \cdot i + b) \bmod m$ & 6 bytes \\
23 & FIBONACCI & Linear recurrence $c_1 v_{i-1} + c_2 v_{i-2}$ & 4 bytes \\
24 & DELTA\_WIDE & u32 values with i8/i16 deltas & $6 + (n-1) \times dw$ bytes \\
7 & RAW & No pattern (fallback) & $S$ bytes \\
8 & RAW\_COMPRESSED & RAW with deflate applied & $3 + n_c$ bytes \\
\bottomrule
\end{tabular}
\caption{Pattern types and their storage costs. $S$ = sectors per track, $d$ = polynomial degree, $k$ = FFT components, $e$ = exceptions, $s$ = segments, $r$ = runs, $n_c$ = compressed size.}
\end{table}

\subsection{Wide-Word Analysis}

A key innovation is reinterpreting byte tracks as arrays of wider integer or floating-point types. A track of 128 bytes that appears random at the byte level may contain 32 linearly-incrementing 32-bit addresses:
\[
\texttt{[0x80000000, 0x80000020, 0x80000040, \ldots]}
\]
The LINEAR\_WIDE detector unpacks bytes as little-endian 16-bit or 32-bit unsigned integers and checks for arithmetic sequences. LINEAR\_F64 and DELTA\_F64 do the same for IEEE 754 double-precision values, with byte-exact reconstruction verification to guarantee lossless compression.

\subsection{Lossless Guarantee}

All pattern detectors verify byte-exact reconstruction before accepting a match. For floating-point patterns (LINEAR\_F64, DELTA\_F64), the reconstructed bytes are compared against the original---not just the numerical values---ensuring no precision loss from floating-point arithmetic.

\section{Binary Format}

\begin{lstlisting}
Header (12 bytes):
  [4] Magic "PLTZ"
  [4] Original data length (uint32 LE)
  [2] Sectors per track (uint16 LE)
  [2] Number of platters (uint16 LE)

Per platter:
  [2] Number of entries (uint16 LE)
  Per entry (individual track):
    [2] Track index (uint16 LE)
    [1] Pattern tag (uint8)
    [...] Pattern-specific payload
  Per entry (meta-group):
    [2] Sentinel 0xFFFF
    [1] META_TAG (12)
    [1] Inner pattern type
    [2] Track count
    [2*N] Track indices
    [1] Sub-tag (0=AllSame, 1=ConstLinear, 2=LinearStartsLinear)
    [...] Meta parameters
\end{lstlisting}

The format has a fixed 12-byte header overhead. Each individual track costs 3 bytes for its index and tag, plus the pattern-specific payload. Meta-groups amortize the per-track overhead across all member tracks.

\section{Experimental Results}

\subsection{Structured Data (PLTZ Advantage)}

\begin{table}[H]
\centering
\begin{tabular}{@{}lrrrr@{}}
\toprule
\textbf{Data} & \textbf{Original} & \textbf{PLTZ} & \textbf{zlib} & \textbf{Improvement} \\
\midrule
Linear ramp (byte) 4KB & 4,096 B & 126 B & 315 B & 2.5$\times$ \\
Linear f64 epochs 8KB & 8,192 B & 350 B & 1,881 B & 5.4$\times$ \\
Segment addresses (u32) 4KB & 4,096 B & 182 B & 2,244 B & 12.3$\times$ \\
Firmware (padding + headers) 4KB & 4,096 B & 65 B & 307 B & 4.7$\times$ \\
Linear ramps 16KB & 16,384 B & 153 B & 408 B & 2.7$\times$ \\
Mixed structured + random 4KB & 4,096 B & 2,150 B & 2,381 B & 1.1$\times$ \\
\bottomrule
\end{tabular}
\caption{PLTZ vs.\ zlib on mathematically structured data. Cross-track meta-descriptors enabled.}
\end{table}

\subsection{Unstructured Data (PLTZ Disadvantage)}

\begin{table}[H]
\centering
\begin{tabular}{@{}lrrrl@{}}
\toprule
\textbf{Data} & \textbf{Original} & \textbf{PLTZ} & \textbf{zlib} & \textbf{Reason} \\
\midrule
English text & 1,580 B & 1,656 B & 680 B & Statistical, not mathematical \\
Random data & 4,096 B & 4,134 B & 4,109 B & Incompressible; 1\% overhead \\
Constant fill 64KB & 65,536 B & 278 B & 84 B & zlib RLE more byte-efficient \\
\bottomrule
\end{tabular}
\caption{Cases where standard compressors outperform PLTZ.}
\end{table}

\subsection{Domain-Specific Benchmarks}

\subsubsection{U-Boot Firmware}

Per-section analysis of a synthetic 512KB U-Boot image shows PLTZ winning on structured sections:
\begin{itemize}
\item Vector table: 36B vs.\ zlib 88B (2.4$\times$ better)
\item PLL configuration: 36B vs.\ zlib 201B (5.6$\times$ better)
\item Relocation table: 102B vs.\ zlib 576B (5.6$\times$ better)
\end{itemize}

\subsubsection{SPICE Ephemeris Kernels (JPL)}

For JPL SPICE kernel structures:
\begin{itemize}
\item Linear epoch arrays: 350B vs.\ zlib 1,881B (5.4$\times$ better)
\item Segment address directories: 182B vs.\ zlib 2,244B (12.3$\times$ better)
\item Chebyshev polynomial coefficients: PLTZ falls through to RAW; LZMA wins
\end{itemize}

\subsubsection{Cross-Track Impact}

The cross-track meta-descriptor optimization provides significant additional compression on data with many identical tracks:
\begin{itemize}
\item 64KB constant fill: 526B $\rightarrow$ 278B (1.9$\times$ improvement over per-track encoding)
\item 16KB linear ramps: 462B $\rightarrow$ 153B (3$\times$ improvement)
\item 4KB firmware image: 90B $\rightarrow$ 65B (1.4$\times$ improvement)
\end{itemize}

\section{Target Applications}

PLTZ is designed for domains where data originates from mathematical processes:

\begin{enumerate}
\item \textbf{Embedded firmware}: Flash images with 0xFF padding, address tables, calibration ramps
\item \textbf{Satellite telemetry}: Housekeeping data, attitude quaternions, epoch arrays, CCSDS framing
\item \textbf{Scientific instruments}: ADC/DAC sweeps, spectral calibration, sensor linearization tables
\item \textbf{SPICE kernels}: Epoch directories, segment address tables, orientation polynomials
\item \textbf{Protocol framing}: Sync patterns, incrementing sequence numbers, structured headers
\item \textbf{Lookup tables}: Gamma curves, PWM tables, motor control profiles
\end{enumerate}

\section{Implementation}

The primary implementation is written in Rust with a bzip2-style command-line interface:

\begin{lstlisting}[language=bash]
pltz file.bin           # compress -> file.bin.pltz
pltz -d file.bin.pltz   # decompress -> file.bin
pltz -k file.bin        # keep original
pltz --no-zlib file.bin # skip all raw compression (fastest)
pltz --raw fast file.bin # zlib only (fast)
pltz --raw good file.bin # zstd (better ratio)
pltz --raw best file.bin # try zlib+zstd+brotli, pick smallest (default)
pltz -c file.bin        # output to stdout (pipeable)
pltz -t file.bin.pltz   # test integrity
pltz -r 21 file.bin     # columnar: input has 21-byte records
pltz -C 64k file.bin    # chunked mode (per-section geometry)
pltz -C auto file.bin   # auto-detect chunk size
pltz -b file.bin        # auto-select best compression settings
pltz --analyze file.bin # analyze data and recommend settings
pltz -j 4 file.bin      # limit to 4 threads
pltz -j 50% file.bin    # use 50% of available cores
\end{lstlisting}

The binary is approximately 1MB stripped, with no runtime dependencies beyond libc. All 23 pattern types are implemented in Rust, with periodic detection using the \texttt{rustfft} library (O($n \log n$) FFT, tracks up to 4096 bytes). Pattern analysis is parallelized via \texttt{rayon} (per-track and per-geometry), yielding a 9$\times$ speedup on multi-core systems. Thread count is configurable via \texttt{-j} (specific count, percentage, or unlimited). An entropy fast-path skips expensive detectors (FFT, polynomial fitting) on high-entropy tracks ($>$200 unique byte values) and skips zstd/brotli on raw tracks with $\geq$150 unique bytes, reducing worst-case latency by 27$\times$ on random data without affecting compression ratio on structured data. The implementation includes 48 unit tests covering all pattern types, cross-track optimization, columnar transforms, and structured encryption. A complete wire format specification is provided in \texttt{doc/WIRE\_FORMAT.md}.

\section{Comparison with Existing Work}

\begin{table}[H]
\centering
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Algorithm} & \textbf{Strategy} & \textbf{Best Domain} \\
\midrule
zlib (DEFLATE) & LZ77 + Huffman & Repeated substrings \\
bzip2 & BWT + RLE + Huffman & Long-range repetition \\
LZMA & Large dictionary + range coding & General purpose \\
\textbf{PLTZ} & \textbf{Function fitting on virtual disk geometry} & \textbf{Mathematical structure} \\
\bottomrule
\end{tabular}
\caption{Algorithmic comparison.}
\end{table}

PLTZ occupies a unique niche: it recognizes \emph{semantic} structure (``this is a linear function'') rather than \emph{syntactic} structure (``these bytes appeared before''). The two approaches are complementary---PLTZ with a configurable raw fallback (deflate, zstd, or brotli) combines the strengths of both.

\section{Columnar Pre-Processing}

For record-oriented data (IoT telemetry, database rows, fixed-size structs), a columnar transform transposes the data so that each byte-position across all records is contiguous:

\begin{lstlisting}
Input (row-major):  [R0F0 R0F1 R0F2] [R1F0 R1F1 R1F2] ...
Output (col-major): [R0F0 R1F0 R2F0 ...] [R0F1 R1F1 R2F1 ...] ...
\end{lstlisting}

The \texttt{-r} (record size) flag tells PLTZ that the input consists of fixed-size records of the given byte length. PLTZ transposes the data so that byte 0 of every record is contiguous, then byte 1 of every record, and so on. This turns interleaved multi-field records into per-field columns that PLTZ's pattern detectors can recognize.

For example, given 100 IoT readings of 21 bytes each (4-byte timestamp + 4-byte device\_id + 2-byte sequence + \ldots), running \texttt{pltz -r 21} groups all first-bytes together, all second-bytes together, etc. The 4 timestamp bytes across 100 records form a 400-byte column that LINEAR\_WIDE detects as a linear sequence. The result:
\begin{itemize}
\item Timestamp column: LINEAR\_WIDE (incrementing by sample interval)
\item Device ID column: CONST (same device)
\item Sequence column: LINEAR\_WIDE (incrementing by 1)
\item Battery column: DELTA (slowly decreasing)
\item Temperature column: DELTA (slowly varying)
\end{itemize}

The format uses magic \texttt{PLTC} to signal that the inverse transform must be applied after decompression. Decompression is transparent---\texttt{pltz -d} auto-detects the columnar wrapper.

When using \texttt{pltz -b} (auto-optimize), the stride is detected automatically via byte-equality autocorrelation: for each candidate stride $s$, the encoder counts positions where $\texttt{data}[i] = \texttt{data}[i+s]$. High correlation indicates a fixed-size record with constant fields repeating at interval $s$. This correctly identifies record sizes for IoT telemetry (21 bytes), GPS fixes (16 bytes), and CCSDS packets (30 bytes) without user input.

\section{Structured Encryption}

\begin{center}
\fbox{\parbox{0.9\textwidth}{\textbf{WARNING:} The current implementation uses a simplified stream cipher for demonstration purposes only. It is NOT cryptographically secure. Production use requires a proper AEAD construction (ChaCha20-Poly1305 or AES-256-GCM).}}
\end{center}

\subsection{Motivation}

Standard encryption and compression are fundamentally at odds:
\begin{itemize}
\item \textbf{Encrypt-then-compress}: Ciphertext is indistinguishable from random; no compression possible.
\item \textbf{Compress-then-encrypt}: Achieves both, but compressed size leaks information about plaintext entropy (CRIME/BREACH attacks).
\end{itemize}

PLTZ offers a third approach: compress first via pattern detection, then encrypt the compressed representation. Because the compressed form consists of (a) structural metadata (pattern types, track layout) and (b) parameter values, these can be encrypted independently, enabling multiple security/efficiency tradeoffs.

\subsection{Three Encryption Modes}

\begin{description}
\item[Structured (PLTE)] Encrypts only the parameter values. Pattern types remain visible. Smallest output. Suitable when the data schema is public but values are private.

\item[Full (PLTF)] Encrypts both metadata and parameters in two separate streams using different counter offsets from the same key. No information leakage except total compressed size. Only 4 bytes larger than structured mode.

\item[Dual-key (PLTD)] Encrypts metadata with an \emph{outer key} and parameters with an \emph{inner key}. Enables tiered access control:
\begin{itemize}
  \item Outer key only: observer sees structure (``track 0 is CONST, track 2 is LINEAR'') but cannot access values.
  \item Both keys: full decryption and data reconstruction.
  \item Use case: operators verify data format/integrity; analysts access measurements.
\end{itemize}
\end{description}

\subsection{Results}

\begin{table}[H]
\centering
\begin{tabular}{@{}lrl@{}}
\toprule
\textbf{Approach} & \textbf{4KB linear ramp} & \textbf{Security} \\
\midrule
Encrypt-then-compress & 4,096 B & IND-CPA (full) \\
Compress-then-encrypt & $\sim$315 B & Leaks compressed size \\
\textbf{PLTZ structured (PLTE)} & \textbf{168 B} & Leaks pattern types + compressed size \\
\textbf{PLTZ full (PLTF)} & \textbf{172 B} & Leaks compressed size only \\
\textbf{PLTZ dual-key (PLTD)} & \textbf{172 B} & Leaks compressed size; outer key reveals structure \\
\bottomrule
\end{tabular}
\caption{Comparison of encryption+compression approaches. PLTZ modes achieve 24:1 compression while encrypted. All compress-then-encrypt schemes leak compressed size; only encrypt-then-compress avoids this.}
\end{table}

\subsection{How It Works}

\begin{enumerate}
\item \textbf{Analyze}: Detect mathematical patterns in the plaintext (same as normal compression).
\item \textbf{Serialize}: Convert each track's pattern parameters to bytes.
\item \textbf{Encrypt stream 1} (parameters): Encrypt serialized parameters with the data key (or inner key in dual-key mode).
\item \textbf{Encrypt stream 2} (metadata): Encrypt pattern types + track indices + geometry with the same key (PLTF) or a separate outer key (PLTD). In structured mode (PLTE), this stream is left unencrypted.
\item \textbf{Output}: Header + encrypted metadata stream + encrypted parameter stream.
\end{enumerate}

Decryption reverses the process: decrypt metadata to learn pattern types, decrypt parameters to get values, evaluate mathematical functions to reconstruct original data.

\subsection{Security Model}

\textbf{Structured mode (PLTE)} leaks:
\begin{itemize}
\item Pattern types per track (e.g., ``track 5 is LINEAR'')
\item Number of tracks, geometry, file size
\end{itemize}

\textbf{Full mode (PLTF)} leaks only:
\begin{itemize}
\item Total compressed size (unavoidable without padding)
\end{itemize}

\textbf{Dual-key mode (PLTD)} leaks:
\begin{itemize}
\item To outer key holder: pattern types and track layout (structure only, no values)
\item To both key holders: full access (no leakage beyond total compressed size)
\item Without any key: total compressed size only (same as PLTF)
\end{itemize}

\textbf{On compressed size leakage:} All compress-then-encrypt schemes leak the compressed size, which reveals information about plaintext entropy and structure. An attacker observing that a 4KB input compresses to 66 bytes can infer the data is highly structured (e.g., constant-filled firmware padding). This is the same class of side channel exploited by CRIME/BREACH attacks on TLS. Mitigations include padding output to fixed block sizes (at the cost of compression ratio) or using encrypt-then-compress (which eliminates compression entirely). For PLTZ's target domains (IoT telemetry, satellite housekeeping), the data schema is typically public and the compressed size reveals little beyond what the attacker already knows.

\begin{center}
\fbox{\parbox{0.85\textwidth}{\textbf{WARNING:} The current implementation uses a simplified stream cipher for demonstration. Production use requires ChaCha20-Poly1305 or AES-256-GCM with proper authentication tags.}}
\end{center}

\section{Image Codec (PLTI)}

The platter model extends naturally to 2D image compression by treating each image row as a track. The PLTI codec supports both lossless and lossy modes for 8-bit and 16-bit grayscale imagery.

\subsection{Lossy Mode}

In lossy mode, each row is fit with the simplest function whose maximum per-pixel error does not exceed a quality threshold $q$:
\begin{enumerate}
\item CONST --- mean value (flat regions, dark frames)
\item LINEAR --- least-squares line (illumination gradients)
\item POLY2/POLY3 --- quadratic/cubic fit (vignetting, smooth curves)
\item RAW --- uncompressed fallback (textured regions)
\end{enumerate}

Cross-row optimization groups rows with identical encodings into a single meta-descriptor. For star field images where 95\%+ of rows are all-black, the entire frame compresses to a single meta-group.

\subsection{Results}

\begin{table}[H]
\centering
\begin{tabular}{@{}lrrr@{}}
\toprule
\textbf{Image type} & \textbf{Original} & \textbf{Compressed} & \textbf{Ratio} \\
\midrule
Star field 256$\times$64 (8-bit, $q$=5) & 16,384 B & 930 B & 17.6:1 \\
Illumination gradient 512$\times$16 ($q$=2) & 8,192 B & 72 B & 113:1 \\
Dark calibration frame 128$\times$32 (16-bit, $q$=4) & 8,192 B & 95 B & 86:1 \\
\bottomrule
\end{tabular}
\caption{PLTI lossy compression on space-relevant imagery.}
\end{table}

\subsection{Target Applications}

\begin{description}
\item[Space probe cameras:] Star fields (95\% black background), calibration dark frames (near-constant), flat fields (polynomial vignetting), illumination gradients. These image types are dominated by mathematical structure that PLTI exploits directly.

\item[Security/surveillance cameras:] Static scenes where 90--99\% of pixels do not change between frames. Difference frames (current minus reference) become arrays of zeros with sparse motion residuals. Static difference frames compress to $\sim$100 bytes regardless of resolution. Only frames containing actual motion require significant storage.

\item[Scientific imaging:] Thermal cameras (smooth temperature gradients), spectrograms (periodic structure per row), oscilloscope captures (waveforms are mathematical functions by definition).
\end{description}

\subsection{Video Codec (PLTV)}

The image codec extends to video with temporal prediction. PLTV uses YCbCr 4:2:0 color, 16$\times$16 block-based motion compensation ($\pm$16 pixel search), and B-frames (bidirectional prediction):

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
\textbf{Scenario} & \textbf{PLTV} & \textbf{H.264} & \textbf{MJPEG} \\
\midrule
Noisy surveillance ($q$=30) & \textbf{22.8:1} & 11.7:1 & 11.5:1 \\
Noisy surveillance ($q$=50) & \textbf{63.5:1} & 11.7:1 & 11.5:1 \\
Clean synthetic & 78:1 & \textbf{1874:1} & 95:1 \\
\bottomrule
\end{tabular}
\caption{PLTV vs.\ H.264 and MJPEG on 2-minute surveillance video (160$\times$120, 5fps).}
\end{table}

PLTV wins on real-world noisy surveillance because sensor noise defeats H.264's motion estimation (it encodes noise as ``motion''). PLTV's quality threshold absorbs the noise, collapsing static regions to CONST regardless of noise level. H.264 wins on clean synthetic content where its block matching and DCT are ideal.

\subsection{Audio Codec (PLTA)}

The platter model also applies to PCM audio: each audio frame (e.g., 256 samples) is treated as one track. The PLTA codec supports both lossless and lossy modes:

\begin{description}
\item[Lossless (quality=0):] Each frame is independently PLTZ-compressed. Silence becomes CONST, linear ramps become LINEAR, pure tones become PERIODIC.
\item[Lossy (quality=1--255):] Each frame is treated as a 1-row PLTI image with the given per-sample error threshold. Supports 8-bit and 16-bit PCM.
\end{description}

\textbf{Target applications:} Synthesized tones, calibration signals, telemetry audio, silence detection, simple waveforms. Not intended for music or speech (use Opus/AAC for those).

\textbf{Format:} Uses magic \texttt{PLTA} with a 12-byte header (sample rate, bit depth, channels, quality, frame size, frame count), followed by independently-compressed frames.

\section{Limitations and Future Work}

\textbf{Current limitations:}
\begin{itemize}
\item Periodic detection limited to tracks $\leq$ 4096 bytes (diminishing returns beyond)
\item Slower than zlib on incompressible data due to adaptive geometry search (7 sector sizes), though entropy fast-path mitigates this (19--30ms per 4KB vs.\ sub-1ms for zlib)
\item Cross-track requires $\geq$4 tracks with same pattern to activate
\item Structured encryption uses demo cipher (not production-ready)
\end{itemize}

\textbf{Recently completed:}
\begin{itemize}
\item Per-section geometry via chunked mode (\texttt{pltz -C 64k}): each chunk independently selects optimal geometry
\item Streaming/chunked encoding (\texttt{pltz -C auto}): bounded memory, random access to individual chunks
\item File format versioning: PLTS v2 header with version byte; decoder rejects unknown future versions
\item Human-readable size arguments: \texttt{64k}, \texttt{1M}, \texttt{auto}
\item Auto-optimization: \texttt{pltz -b} tries multiple configurations and picks the smallest output; \texttt{pltz --analyze} reports recommendations without compressing
\item Audio codec (PLTA): PCM frame-based compression for calibration signals and telemetry audio
\item C FFI bindings (\texttt{clib/}): \texttt{libpltz.so}/\texttt{libpltz.dylib} with compress/decompress/size API
\item Compressed filesystem prototype (\texttt{pltzfs/}): multi-platter storage with per-type platters
\item Configurable thread pool (\texttt{-j N}, \texttt{-j 50\%}, \texttt{-j unlimited})
\item Entropy fast-path: skips expensive detectors and compressors on high-entropy data (27$\times$ faster on random data)
\item Parallel chunk encoding and parallel best-mode search
\end{itemize}

\textbf{Future work:}
\begin{itemize}
\item Production AEAD integration for structured encryption (ChaCha20-Poly1305)
\item Higher-resolution video benchmarks and scene change detection
\end{itemize}

\section{Visualization}

The \texttt{pltz --visualize} command generates SVG diagrams showing the disk platter layout with color-coded tracks. Figure~\ref{fig:uboot} shows a U-Boot SPL header (4KB) and Figure~\ref{fig:mixed} shows mixed structured/random data.

\definecolor{cconst}{HTML}{4CAF50}
\definecolor{clinear}{HTML}{2196F3}
\definecolor{clinwide}{HTML}{3F51B5}
\definecolor{crepeat}{HTML}{9C27B0}
\definecolor{csparse}{HTML}{FFC107}
\definecolor{cdelta}{HTML}{00BCD4}
\definecolor{crawdefl}{HTML}{616161}
\definecolor{crawraw}{HTML}{BDBDBD}

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=0.55]
  % U-Boot SPL: Platter 0 — mostly CONST (green)
  \node[font=\footnotesize] at (0,-5) {Platter 0 (Structured)};
  % 53 CONST tracks (green rings)
  \foreach \i in {1,...,53} {
    \pgfmathsetmacro{\r}{4.2 - \i*0.06}
    \draw[cconst, line width=1pt] (0,0) circle (\r cm);
  }
  % 8 LINEAR_WIDE tracks (blue)
  \foreach \i in {54,...,61} {
    \pgfmathsetmacro{\r}{4.2 - \i*0.06}
    \draw[clinwide, line width=1pt] (0,0) circle (\r cm);
  }
  % 2 REPEAT tracks (purple)
  \foreach \i in {62,63} {
    \pgfmathsetmacro{\r}{4.2 - \i*0.06}
    \draw[crepeat, line width=1pt] (0,0) circle (\r cm);
  }
  \fill[white] (0,0) circle (0.3);
  \node[font=\tiny] at (0,0) {63};

  % Platter 1 (raw) — 1 track
  \begin{scope}[xshift=9cm]
  \node[font=\footnotesize] at (0,-5) {Platter 1 (Raw)};
  \draw[crawraw, line width=8pt] (0,0) circle (3cm);
  \fill[white] (0,0) circle (0.3);
  \node[font=\tiny] at (0,0) {1};
  \end{scope}

  % Legend
  \begin{scope}[xshift=14cm, yshift=1cm]
  \node[font=\small\bfseries, anchor=west] at (0,2.5) {U-Boot SPL (4KB)};
  \fill[cconst] (0,1.6) rectangle (0.4,1.9); \node[font=\scriptsize, anchor=west] at (0.5,1.75) {CONST (53)};
  \fill[clinwide] (0,0.9) rectangle (0.4,1.2); \node[font=\scriptsize, anchor=west] at (0.5,1.05) {LINEAR\_WIDE (8)};
  \fill[crepeat] (0,0.2) rectangle (0.4,0.5); \node[font=\scriptsize, anchor=west] at (0.5,0.35) {REPEAT (2)};
  \fill[crawraw] (0,-0.5) rectangle (0.4,-0.2); \node[font=\scriptsize, anchor=west] at (0.5,-0.35) {RAW (1)};
  \node[font=\scriptsize, anchor=west] at (0,-1.2) {4096B $\rightarrow$ 410B (10:1)};
  \end{scope}
\end{tikzpicture}
\caption{U-Boot SPL header: green rings are 0xFF padding (CONST), blue are vector/relocation tables (LINEAR\_WIDE), purple are repeating patterns. Only 1 of 64 tracks is unstructured.}
\label{fig:uboot}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=0.55]
  % Mixed demo: Platter 0 — various patterns
  \node[font=\footnotesize] at (0,-5) {Platter 0 (Structured)};
  % 4 LINEAR (blue)
  \foreach \i in {1,...,4} {
    \pgfmathsetmacro{\r}{4.0 - \i*0.28}
    \draw[clinear, line width=4pt] (0,0) circle (\r cm);
  }
  % 2 CONST (green)
  \foreach \i in {5,6} {
    \pgfmathsetmacro{\r}{4.0 - \i*0.28}
    \draw[cconst, line width=4pt] (0,0) circle (\r cm);
  }
  % 2 REPEAT (purple)
  \foreach \i in {7,8} {
    \pgfmathsetmacro{\r}{4.0 - \i*0.28}
    \draw[crepeat, line width=4pt] (0,0) circle (\r cm);
  }
  % 2 SPARSE (yellow)
  \foreach \i in {9,10} {
    \pgfmathsetmacro{\r}{4.0 - \i*0.28}
    \draw[csparse, line width=4pt] (0,0) circle (\r cm);
  }
  % 2 LINEAR_WIDE (indigo)
  \foreach \i in {11,12} {
    \pgfmathsetmacro{\r}{4.0 - \i*0.28}
    \draw[clinwide, line width=4pt] (0,0) circle (\r cm);
  }
  % 1 DELTA (cyan)
  \draw[cdelta, line width=4pt] (0,0) circle (0.36cm);
  \fill[white] (0,0) circle (0.15);

  % Platter 1 (raw) — 4 tracks
  \begin{scope}[xshift=9cm]
  \node[font=\footnotesize] at (0,-5) {Platter 1 (Raw)};
  \draw[crawraw, line width=10pt] (0,0) circle (3.2cm);
  \draw[crawraw, line width=10pt] (0,0) circle (2.2cm);
  \draw[crawdefl, line width=10pt] (0,0) circle (1.2cm);
  \draw[crawdefl, line width=10pt] (0,0) circle (0.4cm);
  \fill[white] (0,0) circle (0.15);
  \end{scope}

  % Legend
  \begin{scope}[xshift=14cm, yshift=2cm]
  \node[font=\small\bfseries, anchor=west] at (0,2.5) {Mixed Data (2.8KB)};
  \fill[clinear] (0,1.7) rectangle (0.4,2.0); \node[font=\scriptsize, anchor=west] at (0.5,1.85) {LINEAR (4)};
  \fill[cconst] (0,1.0) rectangle (0.4,1.3); \node[font=\scriptsize, anchor=west] at (0.5,1.15) {CONST (2)};
  \fill[crepeat] (0,0.3) rectangle (0.4,0.6); \node[font=\scriptsize, anchor=west] at (0.5,0.45) {REPEAT (2)};
  \fill[csparse] (0,-0.4) rectangle (0.4,-0.1); \node[font=\scriptsize, anchor=west] at (0.5,-0.25) {SPARSE (2)};
  \fill[clinwide] (0,-1.1) rectangle (0.4,-0.8); \node[font=\scriptsize, anchor=west] at (0.5,-0.95) {LINEAR\_WIDE (2)};
  \fill[cdelta] (0,-1.8) rectangle (0.4,-1.5); \node[font=\scriptsize, anchor=west] at (0.5,-1.65) {DELTA (1)};
  \fill[crawdefl] (0,-2.5) rectangle (0.4,-2.2); \node[font=\scriptsize, anchor=west] at (0.5,-2.35) {RAW deflated (2)};
  \fill[crawraw] (0,-3.2) rectangle (0.4,-2.9); \node[font=\scriptsize, anchor=west] at (0.5,-3.05) {RAW stored (2)};
  \end{scope}
\end{tikzpicture}
\caption{Mixed data across two platters. Platter 0 shows diverse pattern types; Platter 1 distinguishes deflate-compressed (dark) from incompressible (light) raw tracks.}
\label{fig:mixed}
\end{figure}

\section{Conclusion}

PolynomiaZ demonstrates that domain-specific compression based on mathematical function fitting can dramatically outperform general-purpose algorithms on structured numerical data. By modeling data as points on a virtual disk platter, fitting functions per track, compressing parameter metadata across tracks, and optionally encrypting only the minimal parameter bytes, PLTZ achieves 5--12$\times$ better compression than zlib on linear sequences, address tables, and epoch arrays---exactly the data structures found in firmware, telemetry, and scientific computing. The columnar pre-processing transform extends this advantage to record-oriented data, the structured encryption scheme demonstrates that compression and encryption need not be mutually exclusive, the PLTI image codec achieves 17--113$\times$ compression on space-relevant imagery, the PLTV video codec beats H.264 by 2$\times$ on noisy surveillance footage, and the PLTA audio codec provides efficient compression for calibration signals and telemetry audio. C FFI bindings and a compressed filesystem prototype demonstrate the codec's applicability as a building block for larger systems.

\appendix

\section{Wire Format Specification}
\label{app:wireformat}

All multi-byte integers are little-endian unless noted otherwise.

\subsection{Container Formats}

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Magic} & \textbf{Format} & \textbf{Description} \\
\midrule
\texttt{PLTZ} & Standard & Single-geometry compressed data \\
\texttt{PLTC} & Columnar & Columnar-transformed wrapper \\
\texttt{PLTS} & Streaming & Chunked with per-chunk geometry \\
\texttt{PLTI} & Image & Lossy/lossless image codec \\
\texttt{PLTV} & Video & Lossy color video (YCbCr 4:2:0, motion compensation) \\
\texttt{PLTA} & Audio & PCM audio codec (lossless + lossy) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{PLTZ Header (12 bytes)}

\begin{lstlisting}
[4] Magic "PLTZ"
[4] Original data length (uint32 LE)
[2] Sectors per track (uint16 LE)
[2] Number of platters (uint16 LE)
\end{lstlisting}

Per platter: \texttt{[2] num\_entries (uint16 LE)}, followed by entries.

\subsection{Individual Track Entry}

\begin{lstlisting}
[2] Track index (uint16 LE)
[1] Pattern tag (uint8)
[...] Pattern-specific payload
\end{lstlisting}

\subsection{Meta-Group Entry}

\begin{lstlisting}
[2] Sentinel 0xFFFF
[1] META_TAG (12)
[1] Inner pattern type
[2] Track count N (uint16 LE)
[2*N] Track indices
[1] Sub-tag (0=AllSame, 1=ConstLinear, 2=LinearStartsLinear)
[...] Meta parameters
\end{lstlisting}

\subsection{Pattern Tag Payloads}

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}rll@{}}
\toprule
\textbf{Tag} & \textbf{Name} & \textbf{Payload} \\
\midrule
0 & CONST & \texttt{value(u8)} \\
1 & LINEAR & \texttt{start(i16) + step(i16)} \\
2 & RLE & \texttt{num\_runs(u16) + [value(u8)+count(u8)]$\times$N} \\
3 & REPEAT & \texttt{pattern\_len(u16) + bytes} \\
4 & DELTA & \texttt{start(i16) + deltas(i8$\times$(S-1))} \\
5 & PERIODIC & \texttt{num\_comp(u16) + n(u16) + [idx(u16)+re(f64)+im(f64)]$\times$K} \\
6 & POLY & \texttt{degree(u8) + coeffs(f64$\times$(d+1))} \\
7 & RAW & \texttt{raw\_bytes(S bytes)} \\
8 & RAW\_COMPRESSED & \texttt{method(u8) + len(u16) + data} \\
9 & LINEAR\_WIDE & \texttt{width(u8) + count(u16) + start(i64) + step(i64)} \\
10 & LINEAR\_F64 & \texttt{count(u16) + start(f64) + step(f64)} \\
11 & DELTA\_F64 & \texttt{count(u16) + start(f64) + deltas(f32$\times$(n-1))} \\
13 & XOR\_MASK & \texttt{mask(u8) + inner\_tag(u8) + inner\_payload} \\
14 & SPARSE & \texttt{fill(u8) + count(u16) + [idx(u16)+val(u8)]$\times$N} \\
15 & PIECEWISE\_LIN & \texttt{num\_seg(u8) + [idx(u16)+val(i16)+step(i16)]$\times$N} \\
16 & MIRROR & \texttt{half\_data(S/2 bytes)} \\
17 & INTERLEAVED\_LIN & \texttt{count(u16) + startA(i16)+stepA(i16)+startB(i16)+stepB(i16)} \\
18 & EXPONENTIAL & \texttt{count(u16) + base(f32) + ratio(f32)} \\
19 & DELTA\_RLE & \texttt{start(u8) + num\_runs(u16) + [delta(i8)+count(u8)]$\times$N} \\
20 & BIT\_PACKED & \texttt{bits(u8) + packed\_data($\lceil$n$\times$bits/8$\rceil$ bytes)} \\
21 & DIFF\_TABLE & \texttt{order(u8) + initial(order+1 bytes) + diff(i32)} \\
22 & MOD\_ARITH & \texttt{a(u16) + b(u16) + m(u16)} \\
23 & FIBONACCI & \texttt{seed0(u8) + seed1(u8) + c1(u8) + c2(u8)} \\
24 & DELTA\_WIDE & \texttt{width(u8) + dw(u8) + start(u32) + deltas((n-1)$\times$dw bytes)} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{PLTC Header (Columnar Wrapper)}

\begin{lstlisting}
[4] Magic "PLTC"
[4] Original data length (uint32 LE)
[2] Stride / record size (uint16 LE)
[...] Inner PLTZ-encoded data
\end{lstlisting}

\subsection{PLTS Header (Streaming)}

\begin{lstlisting}
[4] Magic "PLTS"
[1] Format version (uint8, currently 2)
[4] Original data length (uint32 LE)
[4] Chunk size (uint32 LE)
[2] Number of chunks (uint16 LE)
Per chunk: [4] compressed_len(uint32 LE) + PLTZ blob
\end{lstlisting}

\subsection{Limits}

Maximum original data length per PLTZ blob: 4\,GB (uint32). Files exceeding 4\,GB automatically use PLTS chunked mode (64\,MB chunks), giving an effectively unlimited maximum file size. Maximum sectors per track: 65,535 (uint16). Maximum tracks/platters/chunks: 65,535 (uint16).

\end{document}
